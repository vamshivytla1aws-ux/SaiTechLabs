import { redirect } from "next/navigation"; import { StudentLoginForm } from "@/components/student/StudentForms"; import { getAuthenticatedStudent } from "@/lib/student-auth";export const dynamic="force-dynamic";export default async function Login(){if(await getAuthenticatedStudent())redirect("/student/dashboard");return <div className="admin-root admin-login-page student-login"><div className="admin-login-card"><div className="admin-login-mark"><span>SL</span></div><p className="admin-kicker">Student portal</p><h1>Welcome</h1><p>Access your course, attendance, payments, feedback and certificates.</p><StudentLoginForm/></div></div>}
