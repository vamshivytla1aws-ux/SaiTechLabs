import { StudentPasswordForm } from "@/components/student/StudentForms"; import { requireStudent } from "@/lib/student-auth";export default async function Profile(){const a=await requireStudent(),s=a.student;return <div className="admin-page"><div className="admin-page-head"><div><p className="admin-kicker">Account</p><h1>Profile</h1></div></div><div className="admin-settings-grid"><section className="admin-panel"><h2>Personal information</h2><dl className="admin-definition single">{[["Student code",s.studentCode],["Name",s.fullName],["Email",s.email],["Phone",s.phone],["College",s.collegeName],["Qualification",s.qualification],["Graduation year",String(s.graduationYear)]].map(([k,v])=><div key={k}><dt>{k}</dt><dd>{v}</dd></div>)}</dl></section><section className="admin-panel"><h2>Change password</h2><StudentPasswordForm/></section></div></div>}
