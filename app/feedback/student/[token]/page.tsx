import type { Metadata } from "next"; import { FeedbackForm } from "@/components/operations/FeedbackForm"; import { getDb } from "@/lib/db"; import { hashToken } from "@/lib/operations";
export const metadata:Metadata={title:"Student Feedback",robots:{index:false,follow:false}};export const dynamic="force-dynamic";
export default async function StudentFeedback({params}:{params:Promise<{token:string}>}){const{token}=await params;const link=await getDb().publicLink.findUnique({where:{tokenHash:hashToken(token)},include:{batch:{include:{course:true}}}});const valid=link?.type==="STUDENT_FEEDBACK"&&link.isActive&&!link.revokedAt&&(!link.expiresAt||link.expiresAt>new Date())&&(!link.singleUse||!link.usedAt);if(!valid)return <Unavailable/>;return <main className="secure-public-page"><div className="narrow-container container"><p className="eyebrow">Student feedback</p><h1>{link.batch?.course.name??"SaiTech Labs Program"}</h1><p>{link.batch?.name}</p><FeedbackForm endpoint={`/api/feedback/token/${token}`} kind="student"/></div></main>}
function Unavailable(){return <main className="secure-public-page"><div className="narrow-container container"><h1>This link is no longer available.</h1><p>Please contact SaiTech Labs if you need a new feedback link.</p></div></main>}
