import type { Metadata } from "next"; import { FeedbackForm } from "@/components/operations/FeedbackForm"; import { getDb } from "@/lib/db"; import { hashToken } from "@/lib/operations";
export const metadata:Metadata={title:"College Feedback",robots:{index:false,follow:false}};export const dynamic="force-dynamic";
export default async function CollegeFeedback({params}:{params:Promise<{token:string}>}){const{token}=await params;const link=await getDb().publicLink.findUnique({where:{tokenHash:hashToken(token)},include:{batch:{include:{course:true}}}});const valid=link?.type==="COLLEGE_FEEDBACK"&&link.isActive&&!link.revokedAt&&(!link.expiresAt||link.expiresAt>new Date())&&(!link.singleUse||!link.usedAt);if(!valid)return <main className="secure-public-page"><div className="narrow-container container"><h1>This link is no longer available.</h1></div></main>;return <main className="secure-public-page"><div className="narrow-container container"><p className="eyebrow">College feedback</p><h1>{link.collegeName??"Partner college"}</h1><p>{link.batch?.course.name} · {link.batch?.name}</p><FeedbackForm endpoint={`/api/feedback/token/${token}`} kind="college"/></div></main>}
