import type { Metadata } from "next"; import { getDb } from "@/lib/db"; import { hashToken } from "@/lib/operations";
export const metadata:Metadata={title:"Certificate Verification",robots:{index:false,follow:false}};export const dynamic="force-dynamic";
export default async function CertificatePage({params}:{params:Promise<{token:string}>}){const{token}=await params;const cert=await getDb().certificate.findUnique({where:{verificationTokenHash:hashToken(token)},include:{student:true,enrollment:{include:{batch:{include:{course:true}}}}}});if(!cert)return <main className="secure-public-page"><div className="status-card"><h1>Certificate not found.</h1><p>The verification information is unavailable.</p></div></main>;return <main className="secure-public-page"><div className="status-card certificate-card"><p className="eyebrow">SaiTech Labs</p><h1>Certificate Verification</h1><dl><div><dt>Student</dt><dd>{cert.student.fullName}</dd></div><div><dt>Course</dt><dd>{cert.enrollment.batch.course.name}</dd></div><div><dt>Batch</dt><dd>{cert.enrollment.batch.name}</dd></div><div><dt>Certificate number</dt><dd>{cert.certificateNumber}</dd></div><div><dt>Issue date</dt><dd>{cert.issuedAt?new Intl.DateTimeFormat("en-IN",{dateStyle:"long"}).format(cert.issuedAt):"Not issued"}</dd></div><div><dt>Status</dt><dd>{cert.status}</dd></div></dl></div></main>}
