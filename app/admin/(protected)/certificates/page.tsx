import Link from "next/link";
import { RevokeButton } from "@/components/admin/QueueActions";
import { requireAdminPermission } from "@/lib/admin-auth";
import { getDb } from "@/lib/db";

export default async function CertificatesPage(){await requireAdminPermission("certificates:manage");const certificates=await getDb().certificate.findMany({orderBy:{createdAt:"desc"},take:300,include:{student:true,enrollment:{include:{batch:{include:{course:true}}}}}});return <div className="admin-page"><div className="admin-page-head"><div><p className="admin-kicker">Credentials</p><h1>Certificates</h1><p>Issue certificates from student enrollments and revoke invalid credentials.</p></div></div><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Certificate</th><th>Student</th><th>Course / batch</th><th>Issued</th><th>Status</th><th>Action</th></tr></thead><tbody>{certificates.map(certificate=><tr key={certificate.id}><td>{certificate.certificateNumber}</td><td><Link className="admin-row-link" href={`/admin/students/${certificate.studentId}`}>{certificate.student.fullName}</Link><small>{certificate.student.studentCode}</small></td><td>{certificate.enrollment.batch.course.name}<small>{certificate.enrollment.batch.code}</small></td><td>{certificate.issuedAt?.toLocaleDateString("en-IN")||"Not issued"}</td><td>{certificate.status}</td><td>{certificate.status!=="REVOKED"&&<RevokeButton url={`/api/admin/certificates/${certificate.id}/revoke`}/>}</td></tr>)}</tbody></table>{!certificates.length&&<p className="admin-empty">No certificates issued.</p>}</div></div>}
