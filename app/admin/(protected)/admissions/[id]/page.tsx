import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, Mail, MessageCircle, Phone } from "lucide-react";
import { LeadUpdateForm, NoteForm } from "@/components/admin/AdminForms";
import { getDb } from "@/lib/db";
const fmt=(v:Date)=>new Intl.DateTimeFormat("en-IN",{dateStyle:"medium",timeStyle:"short"}).format(v);const display=(v:string)=>v.replaceAll("_"," ");
export default async function AdmissionDetail({params}:{params:Promise<{id:string}>}){const{id}=await params;const row=await getDb().admission.findUnique({where:{id},include:{notes:{orderBy:{createdAt:"desc"},include:{createdBy:{select:{name:true}}}}}});if(!row)notFound();const follow=row.nextFollowUpAt?new Date(row.nextFollowUpAt.getTime()-row.nextFollowUpAt.getTimezoneOffset()*60000).toISOString().slice(0,16):"";return <div className="admin-page"><Link className="admin-back" href="/admin/admissions"><ArrowLeft/>Back to admissions</Link><div className="admin-page-head detail"><div><p className="admin-kicker">Admission lead · {row.id}</p><h1>{row.studentName}</h1><p>Submitted {fmt(row.createdAt)}</p></div><span className={`admin-status status-${row.leadStatus.toLowerCase()}`}>{display(row.leadStatus)}</span></div><div className="admin-detail-grid"><div className="admin-detail-main"><section className="admin-panel"><header><h2>Student information</h2></header><dl className="admin-definition"><Item term="Email" value={row.email}/><Item term="Phone" value={row.phone}/><Item term="Course" value={row.course}/><Item term="Training mode" value={display(row.trainingMode)}/><Item term="Qualification" value={row.qualification}/><Item term="Current status" value={row.currentStatus}/><Item term="Graduation year" value={String(row.graduationYear)}/><Item term="College" value={row.collegeName}/><Item term="State" value={row.state}/><Item term="Consent" value={row.consent?"Provided":"Not provided"}/><Item term="Last contacted" value={row.lastContactedAt?fmt(row.lastContactedAt):"Not recorded"}/><Item term="Next follow-up" value={row.nextFollowUpAt?fmt(row.nextFollowUpAt):"Not scheduled"}/></dl>{row.message&&<div className="admin-message"><strong>Student message</strong><p>{row.message}</p></div>}</section><section className="admin-panel"><header><h2>Internal notes</h2><small>Append-only activity history</small></header><NoteForm id={row.id}/><div className="admin-notes">{row.notes.map(note=><article key={note.id}><p>{note.note}</p><small>{note.createdBy.name} · {fmt(note.createdAt)}</small></article>)}{!row.notes.length&&<p className="admin-empty">No notes added yet.</p>}</div></section></div><aside className="admin-detail-side"><section className="admin-panel"><header><h2>Lead action</h2></header><LeadUpdateForm id={row.id} status={row.leadStatus} followUp={follow}/></section><section className="admin-panel"><header><h2>Contact</h2></header><div className="admin-contact-actions"><a href={`tel:${row.phone}`}><Phone/>Call</a><a href={`https://wa.me/${row.phone.replace(/\D/g,"")}`} target="_blank" rel="noreferrer"><MessageCircle/>WhatsApp</a><a href={`mailto:${row.email}`}><Mail/>Email</a></div></section></aside></div></div>}
function Item({term,value}:{term:string;value:string}){return <div><dt>{term}</dt><dd>{value}</dd></div>}
