import { requireAdminPermission } from "@/lib/admin-auth";
import { getDb } from "@/lib/db";

export default async function AuditPage(){await requireAdminPermission("audit:view");const rows=await getDb().adminAuditLog.findMany({orderBy:{createdAt:"desc"},take:500,include:{admin:true}});return <div className="admin-page"><div className="admin-page-head"><div><p className="admin-kicker">Governance</p><h1>Audit log</h1><p>Immutable record of sensitive operational and access-control changes.</p></div></div><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Timestamp</th><th>Administrator</th><th>Action</th><th>Entity</th><th>Metadata</th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td>{row.createdAt.toLocaleString("en-IN")}</td><td>{row.admin.name}<small>{row.admin.email}</small></td><td>{row.action.replaceAll("_"," ")}</td><td>{row.entityType}<small>{row.entityId||"—"}</small></td><td>{row.metadata?JSON.stringify(row.metadata):"—"}</td></tr>)}</tbody></table>{!rows.length&&<p className="admin-empty">No audit events recorded.</p>}</div></div>}
