import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, Mail, MessageCircle, Phone } from "lucide-react";
import { ContactStatusForm } from "@/components/admin/AdminForms";
import { getDb } from "@/lib/db";
const fmt=(v:Date)=>new Intl.DateTimeFormat("en-IN",{dateStyle:"medium",timeStyle:"short"}).format(v);
export default async function ContactDetail({params}:{params:Promise<{id:string}>}){const{id}=await params;const row=await getDb().contactEnquiry.findUnique({where:{id}});if(!row)notFound();return <div className="admin-page"><Link className="admin-back" href="/admin/contact-enquiries"><ArrowLeft/>Back to enquiries</Link><div className="admin-page-head detail"><div><p className="admin-kicker">Contact enquiry · {row.id}</p><h1>{row.name}</h1><p>Received {fmt(row.createdAt)}</p></div><span className={`admin-status status-${row.status.toLowerCase()}`}>{row.status}</span></div><div className="admin-detail-grid"><section className="admin-panel"><header><h2>Enquiry details</h2></header><dl className="admin-definition"><div><dt>Email</dt><dd>{row.email}</dd></div><div><dt>Phone</dt><dd>{row.phone}</dd></div><div><dt>Last updated</dt><dd>{fmt(row.updatedAt)}</dd></div></dl><div className="admin-message"><strong>Message</strong><p>{row.message}</p></div></section><aside className="admin-detail-side"><section className="admin-panel"><header><h2>Update status</h2></header><ContactStatusForm id={row.id} status={row.status}/></section><section className="admin-panel"><header><h2>Respond</h2></header><div className="admin-contact-actions"><a href={`tel:${row.phone}`}><Phone/>Call</a><a href={`https://wa.me/${row.phone.replace(/\D/g,"")}`} target="_blank" rel="noreferrer"><MessageCircle/>WhatsApp</a><a href={`mailto:${row.email}`}><Mail/>Email</a></div></section></aside></div></div>}
