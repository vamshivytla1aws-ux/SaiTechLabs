import Link from "next/link";
import { RevokeButton } from "@/components/admin/QueueActions";
import { requireAdminPermission } from "@/lib/admin-auth";
import { getDb } from "@/lib/db";

export default async function PublicLinksPage(){await requireAdminPermission("students:manage");const links=await getDb().publicLink.findMany({orderBy:{createdAt:"desc"},take:300,include:{student:true,enrollment:{include:{batch:true}}}});return <div className="admin-page"><div className="admin-page-head"><div><p className="admin-kicker">Secure sharing</p><h1>Public links</h1><p>Token values are stored only as hashes. New URLs are displayed once at creation.</p></div></div><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Created</th><th>Type</th><th>Student</th><th>Batch</th><th>Expires / usage</th><th>Status</th><th></th></tr></thead><tbody>{links.map(link=><tr key={link.id}><td>{link.createdAt.toLocaleString("en-IN")}</td><td>{link.type.replaceAll("_"," ")}</td><td>{link.student?<Link className="admin-row-link" href={`/admin/students/${link.student.id}`}>{link.student.fullName}</Link>:"—"}</td><td>{link.enrollment?.batch.code||"—"}</td><td>{link.expiresAt?.toLocaleDateString("en-IN")||"No expiry"}<small>{link.singleUse?link.usedAt?"Used":"Single use":"Reusable"}</small></td><td>{link.isActive&&!link.revokedAt?"Active":"Revoked"}</td><td>{link.isActive&&!link.revokedAt&&<RevokeButton url={`/api/admin/public-links/${link.id}/revoke`}/>}</td></tr>)}</tbody></table>{!links.length&&<p className="admin-empty">No public links created.</p>}</div></div>}
